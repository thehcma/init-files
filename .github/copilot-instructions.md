# Copilot instructions

**Read [`AGENTS.md`](../AGENTS.md) at the repo root first — it is the single
source of truth for this repository's ground rules, and it tells you to also
load every `alwaysApply: true` rule under `.cursor/rules/`.**

This file is a thin pointer so GitHub Copilot loads the same guidance every other
agent does. Do not duplicate rules here — put them in `AGENTS.md`.

Org template: repository-helpers#574.
